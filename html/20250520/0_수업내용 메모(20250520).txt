1.(수업내용) 상품 정보 -> CSV -> JSON (PapaParse)
  (원래로직) 상품 정보 (백엔드 단: Rest Service) -> JSON

2. Json parse -> 개별 상품 패널 (for ... of)
14rows (컬럼, 공백(마지막)) => 12ros : 배열

3. grid -> grid layout / flex layout : flex=wrap

4. 같은류 버튼들
=> class = "btn" title="${상품아이디}"
=> id = "btn-${상품아이디}"

5. 이벤트에서 e.target을 이용해서 