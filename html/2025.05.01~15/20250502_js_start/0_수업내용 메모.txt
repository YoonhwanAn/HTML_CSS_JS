// 1. 자바스크립트의 역사.
    추가로 잘 알아볼것
    sun사 -> origin사 합병
    ECMA와 xxxx
    node.js(백단 자바스크립트)의 출시로, node.js의 비중이 커지고 있다.
    node.js의 장점 - 프론트,백을 모두 javascript를 사용함으로 유효한 경우가 많음.

// 2. 자바스크립트는 외장 스크립트이기 때문에 <script>라는 html코드를 이용해서 html파일에 삽입.
    있는 위치에서

// 3. 자바스트립트의 예시
    <script>
    var heading = document.qurerySelector('#heading');  // heading이라는 변수 선언 (문서에서 #heading이라는 쿼리 셀렉터)
    heading.onclick = function() {  // heading을 클릭했을 때, 작동하는 함수 선언
        heading.style.color = "red";    // heading의 스타일 칼라를 빨간색으로 변경해라.
    }
    </script>

// 4. resource
    https://developer.mozilla.org/ko/docs/Web/JavaScript
    https://developer.mozilla.org/ko/docs/Web/API

// 5. API doc(기술문서) 보는(검색)법

    var __ = document.querySelector();
    document를 찾아보고 싶다.
    // MDN -> Web API -> Interface(인터페이스) -> Document -> 인스턴스 메서드 -> querySelector()

    // querySelector()
    // ㄴ 인스턴스 메서드
    // 좌측 메뉴에서 찾을 것 (해당 객체 소속(멤버) 속성(필드)/메서드(함수))

// 6. html -> 요소 동적제어 : ECMAScript Level 6 (ES6)

// 7. javascript의 확장자는 ~.js

// 8. document.querySelector(선택자) => 태그 인식(제어)
        (유사) document.getElementById(아이디)

// 9. 잡코리아 이력서 선생님 메일로 보내기