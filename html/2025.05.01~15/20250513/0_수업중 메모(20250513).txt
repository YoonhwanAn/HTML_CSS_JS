// 복습

// 1. 이벤트 => 제어를 자바스크립트로

// 2. click => 리스너(감지자:귀) => 처리자(함수)
      addEventListener    handler
      on~

      태그(요소) on + 이벤트 ="핸들러" => json

// 3. 이벤트 타겟(대상) 요소.on 이벤트 = 이벤트 핸들러
      이벤트 타겟(대상) 요소.addEventListener('이벤트',핸들러()) => 콜백 함수
      => 인자부분에 함수를 넣는게 콜백이다. ?콜백학수의 명확한 정의?

//  진도

// 1. 자바스크립트의 객체
   // (1) 객체란?
      프로그램에서 인식가능한 모든 대상

      낱개의 자료
      => 같은 자료의 복수(다수) 표현 : 배열
      => 다양한 자료 복수(다수) 표현 : 구조체 + OOP
      => 클래스(객체) (넓은 의미) (좁은 의미에서 객체는 인스턴스를 이야기하기도함 / 인스턴스: 객체를 틀처럼 사용해서 같은 모양으로 찍어낸 것)

      클래스는 소속 함수(메서드:method)를 가지고 있음.
      (예) document.write() - document 소속의 함수 write()


   // (2) 내장객체
      https://developer.mozilla.org/ko/docs/Web/JavaScript

   // (3) 문서 객체 모델(DOM)
      World Wide Web Consortium(W3C)에서 규정을 정의해두면 자바, 자바스크립트 등에서 해당 규정에 따라 코드를 생성.
      => js 전용 아님
      js에 적용된 사항이라고 보면 됨?https://developer.mozilla.org/ko/docs/Web/API
      
      => DOM 소개 문서 참고 https://developer.mozilla.org/ko/docs/Web/API/Document_Object_Model/Introduction
      // 쉽게 말하면 HTML을 제어하기 위한 것이다.

   // (4) 웹 브라우저 객체
      작성요

   // (5) 사용자 정의 개체
      작성요.


// 2. 프로퍼티와 메서드
      객체 내에는 프로퍼티와  메서드가 있음.
      (1) 프로퍼티 : 객체의 특징이나 속성 (정적)
      (2) 메서드 : 객체에서 할 수 있는 동작 (동적)

// 3. 엑셀로 프로퍼티와 메서드들을 저장해 둡시다. (cheatsheet)

// 4. 